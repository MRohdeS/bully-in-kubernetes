apiVersion: v1
kind : Service
metadata:
  name: flask-web-service
spec:
  type: NodePort
  selector:
    app: flask-web-app
    leader: "true"
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
